#include <stdio.h>
#include <string.h>


#define BUFFER 1024
int main(int argc, char* argv[]) {
	char userinput;
	char usercount;
	
	char buffer[BUFFER];
	
	
	// Read a word as the first argument
	// Confirm it contains only alpha char
	printf ("Enter a line of text");
	userinput = getchar();
	
	return 0;
}
